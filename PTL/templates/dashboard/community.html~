{% extends "dashboard/base.html" %}

{% block title %}Register with Rango{% endblock %}

{% block body_block %}

<div class="hero-unit">
    <h1>{{ community_name }}</h1>
    <br/>
 <div class="container">

{% if community %}
{% if members %}
<ul>
{% for member in members %}
<li>{{ member.user.username }}</li>
{% endfor %}
</ul>
{% else %}
<strong>No members currently in community.</strong>
{% endif %}
{% else %}
The specified category {{ community_name }} does not exist!
{% endif %}
    </div>
</div>
{% endblock %}

